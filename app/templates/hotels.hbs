<div class='row'>
  <div class='col-md-4'>
    <h3>Select your hotel</h3>
    <ul class="hotel-list">
      {{#each model as |hotel|}}
        {{#link-to 'hotels.hotel' hotel.id}}<li>{{hotel.name}}</li>{{/link-to}}
      {{/each}}
    </ul>
  </div>
  <div class='col-md-8'>
    {{outlet}}
  </div>
</div>
