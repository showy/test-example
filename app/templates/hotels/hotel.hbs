<div class='row'>
  <div class='col-md-6'>
    <h3>Select room</h3>
    <ul>
      {{#each model.rooms as |room| }}
        {{#link-to 'hotels.hotel.room' room.id}}
          <li> 
            {{room.group}}
          </li>
        {{/link-to}}
      {{/each}}
    </ul>
  </div>
  <div class='col-md-6'>
    {{outlet}}
  </div>
</div>